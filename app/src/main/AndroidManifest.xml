<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wireframe">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.resetPassWord"></activity>
        <activity android:name=".activities.category" />
        <activity android:name=".activities.chinese" />
        <activity android:name=".activities.home" />
        <activity android:name=".activities.ExMenu" />
        <activity android:name=".activities.verificationCode" />
        <activity android:name=".activities.ForgotPassword" />
        <activity android:name=".activities.Register3" />
        <activity android:name=".activities.Regiser2" />
        <activity android:name=".activities.Register" />
        <activity android:name=".activities.Login" />
        <activity android:name=".activities.wellcome3" />
        <activity android:name=".activities.wellcome2" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>